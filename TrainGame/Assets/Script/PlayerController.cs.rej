diff a/TrainGame/Assets/Script/PlayerController.cs b/TrainGame/Assets/Script/PlayerController.cs	(rejected hunks)
@@ -44,14 +44,18 @@
 
     void Update()
     {
-        if (LButtonDownFlag)
-        {
-            transform.position -= new Vector3(movingSpeed, 0, 0);
-        }
+        int moveKey = 0;
+        if (LButtonDownFlag) moveKey = -1;
+        if (RButtonDownFlag) moveKey = 1;
+
+
+        // �v���C���[�̑��x
+        float speedX = Mathf.Abs(this.rigid2D.velocity.x);
 
-        if (RButtonDownFlag)
+        // �X�s�[�h����
+        if (speedX < this.maxaWalkSpeed)
         {
-            transform.position += new Vector3(movingSpeed, 0, 0);
+            this.rigid2D.AddForce(transform.right * moveKey * this.walkForce);
         }
 
     }
